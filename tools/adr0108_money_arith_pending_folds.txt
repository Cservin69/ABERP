# ADR-0108 T-8 — the PENDING-FOLD REGISTER.
#
# Format: <file>|<enclosing_fn>|<class>|<arm>|<column>[+<column>...]
#         ('#' comments and blank lines ignored; matching is EXACT and whole-line)
#
# One line per SQL-side arithmetic site that §3.4 schedules for a fold into
# Rust and that has NOT been folded yet. It is a RATCHET, not an allowlist:
#
#   * a finding that is not here reds T8-3 — nothing new may be added to SQL;
#   * an entry here that matches nothing reds T8-4 — when the fold lands, the
#     entry MUST be deleted in the same commit.
#
# So the file can only shrink, and it empties itself. That is deliberate: a
# permanent exemption is how "temporary" becomes "sanctioned" (§3.3 makes the
# same call about N-2's one-entry `f64` allowlist, and §3.2 E's
# `material_inventory` divergence is the same mechanism one product-cycle on).
#
# Records are keyed on the ENCLOSING FUNCTION, never on a line number — the
# ADR-0098 opener-fingerprint and ADR-0106 door-record convention — so an
# unrelated edit that shifts lines cannot red the gate, while moving the SQL to
# a different function reds it correctly.
#
# EXPOSURE TODAY IS ZERO and this is why the register is safe to ship with the
# sites still in it: `aberp-inventory` has NOT crossed the engine seam, so every
# statement below still executes on DuckDB, where `DECIMAL` is a real decimal
# type and none of these coerce. The register's entries are the exact set that
# must empty BEFORE their family crosses.
#
# --- §3.4 sites 1 and 2: the ÁFA report — FOLDED 2026-08-01, entries DELETED --
# Both `reports.rs` sites are gone, and the four + one records with them:
#
#   * `query_outgoing_groups` no longer sums or multiplies in SQL. It projects
#     `quantity` / `unit_price` / `vat_rate_kind` and folds per line in Rust
#     through `aberp_billing::domain::invoice::{line_net_total, line_vat_amount}`
#     — literally the functions `nav_xml::write_summary` sums, so the ÁFA report
#     equals the filing (T-5(c), `adr0108_step7_report_ties_to_filing.rs`).
#   * `query_eur_huf_equivalent` no longer `SUM`s: it projects the R1 column and
#     folds with `checked_add`, and the `decimal_str_to_i64(&s).unwrap_or(0)`
#     that would have printed 0 HUF on an overflow died in the same commit,
#     along with its two siblings.
#
# This is T8-4's ratchet doing its job: leaving the entries here after the fold
# landed would have RED the gate, which is the whole point of a register that
# can only shrink.
#
# M-2, which the ÁFA fold waited on, is CLOSED — Ervin ruled per-line: the
# report shows what was filed.
#
# --- §3.4 sites 3 and 4: the stock-cache rebuild, which is a WRITE ------------
# `SUM(qty_delta)` becomes REAL under R2 and writes a float `stock_qty` back
# into the products cache. `record_movement` is the in-transaction single-product
# rebuild; `rebuild_stock_cache_for_tenant` is the batch one (also the body
# behind `bin/rebuild_stock_cache.rs`, which carries no SQL of its own — §3.4's
# third inventory row cites its usage string, not a statement).
crates/aberp-inventory/src/repository.rs|record_movement|R2|agg-sum|qty_delta
crates/aberp-inventory/src/repository.rs|rebuild_stock_cache_for_tenant|R2|agg-sum|qty_delta
#
# --- the FIVE columns the R2 census had never carried, and the ONE live site --
# ADDED 2026-08-02 (B1). S455 (Step 7 Part C) moved `inventory_balances.
# on_hand_qty` / `reserved_qty` / `committed_qty` / `consumed_qty` and
# `inventory_reservations.qty` out of §3.2 E and into R2 TEXT. The scanner's R2
# census was never widened to match, so for the whole of Parts C-I the gate
# looked at these five columns and saw nothing. Widening it makes exactly ONE
# site visible tree-wide (measured, not assumed — the scan is the gate's own,
# over all 753 statements in 303 files):
#
#   `commit_material_in_tx` does `SET committed_qty = committed_qty + ?1` inside
#   the DEAL saga's commit transaction. Today that is a DuckDB `DOUBLE + DOUBLE`
#   and it is fine. At the family's cutover the column is R2 `TEXT`, and
#   `TEXT + <bound f64>` coerces to REAL — a float written back into a quantity
#   column, which is the exact class R2 exists to prevent, on the write side,
#   inside a transaction. The bound `?1` is an `f64` today as well.
#
# It is registered rather than folded HERE for the reason every other row in
# this file is: the fold is a change to a live DuckDB write on the saga's
# transaction, and §9's own rule is that a migrator-half commit does not touch
# the product's write path — mixed in, it would be indistinguishable from
# migration collateral. EXPOSURE TODAY IS ZERO: `material_inventory.rs` has not
# crossed the seam, so the statement executes on DuckDB.
#
# ⚠ The fold is NOT "sum the same thing in Rust". `commit_material_in_tx`
# already reads the balance row two statements earlier (`read_balance_in_tx_inner`
# at `:562`) for its sufficiency check, so the fold is a read-modify-write on a
# value it is ALREADY holding — and it must stay inside that transaction, for
# the same reason `record_movement`'s does.
apps/aberp/src/material_inventory.rs|commit_material_in_tx|R2|arith-add|committed_qty
#
# --- what is NOT here, and was expected to be --------------------------------
# §3.4's site 7 (`repository.rs:548-549`, `low_stock_products`) and its twin at
# `:585` (`count_low_stock_products`) — the tree's only Q2 lexicographic break.
# Both are ALREADY FOLDED on this branch: the predicate and the deficit ordering
# now run in Rust over `Decimal` in `low_stock_candidates`, and the surviving
# `COALESCE(stock_qty, 0)` there is a PROJECTION, not a comparison. The gate is
# green on them because they are fixed, not because it cannot see them — T8-2's
# liveness fixture runs that exact statement and requires the `cmp-lt`,
# `arith-sub` and `cmp-orderby` arms to fire on it.
