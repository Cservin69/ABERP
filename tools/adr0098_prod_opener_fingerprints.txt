# ADR-0099 (PROD SAFE lane) — FROZEN per-opener fingerprint set (gate CHECK P2).
# Ported from editions ADR-0098 R4 (CHECK 10k). Format: <path>|<fname>:<opener-text>.
# Line numbers are DROPPED so a benign line-shift does not churn the set; any add,
# removal, or content-change of an opener flips the set -> RED. P1 (count) is the
# coarse backstop; P2 (this) also catches a COUNT-PRESERVING intra-file swap.
# Machine-derived from frozen prod commit f7519b4 (tag PROD_v2.27.76).
# ADR-0099 Addendum 2 (2026-07-09, owner Ervin) — re-cut −7: the three deleted
# `derive_nav_invoice_number` billing-store opens (Defect 1) + the four drain
# Ledger::open sites reused via free sync_mirror / Ledger::from_connection
# (Defect 3). See adr0098_prod_frozen_residuals.txt header for the full rationale.
#
# ADR-0099 H3 ratchet: 271 openers after the serve.rs write-fork migration.
#
# ADR-0099 H3 ratchet (wave 2a): 267 openers.
#
# ADR-0099 H3 ratchet (wave 2b): 266 openers.
# ADR-0099 H3 wave-2c (2026-07-09): re-cut −9 — the avl_vendors subsystem's 9
# opener fingerprints (7 serve.rs AVL handlers + append_vendor_event Ledger::open
# + fire_overdue Connection::open) removed; every one now routes through the
# shared Handle (db.write()/db.read()). Removals-only, verified via comm -13.
# ADR-0099 H3 wave-2d (2026-07-09): re-cut -2 — email_invoice record_email_audit_entry
# 2 opener fingerprints (Connection::open + Ledger::open) removed; both now the
# shared Handle. Removals-only, verified via comm -13.
# ADR-0099 H3 wave-2e (2026-07-09): re-cut -1 — append_machine_event Ledger::open
# removed (now the shared Handle). Removals-only, verified via comm -13.
apps/aberp/src/ap_sync.rs|bootstrap_year_already_recorded:let ledger = Ledger::open(db_path, tenant, binary_hash).with_context(|| {
apps/aberp/src/ap_sync.rs|write_cycle_audit_entry_inner:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/ap_sync.rs|write_cycle_audit_entry_inner:let mut conn = duckdb::Connection::open(db_path).with_context(|| {
apps/aberp/src/drain_pending_retries.rs|drive_one_retry:let mut conn = Connection::open(db_path).map_err(|e| {
apps/aberp/src/drain_pending_retries.rs|drive_one_retry:let mut conn = Connection::open(db_path).map_err(|e| {
apps/aberp/src/drain_pending_retries.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/drain_submission_queue.rs|drive_one_invoice:let ledger = Ledger::open(db_path, tenant, binary_hash_bytes).map_err(|e| {
apps/aberp/src/drain_submission_queue.rs|drive_one_invoice:let ledger = Ledger::open(db_path, tenant, binary_hash_bytes).map_err(|e| {
apps/aberp/src/drain_submission_queue.rs|drive_one_invoice:let ledger_tx1 = Ledger::open(db_path, tenant.clone(), binary_hash_bytes).ma
apps/aberp/src/drain_submission_queue.rs|drive_one_invoice:let mut conn = Connection::open(db_path).map_err(|e| {
apps/aberp/src/drain_submission_queue.rs|drive_one_invoice:let mut conn = Connection::open(db_path).map_err(|e| {
apps/aberp/src/drain_submission_queue.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/email_relay_daemon.rs|process_one_row:let conn = Connection::open(&db_path).with_context(|| {
apps/aberp/src/email_relay_daemon.rs|process_one_row:let conn = Connection::open(&db_path2).context("open DB to mark Failed")?;
apps/aberp/src/email_relay_daemon.rs|process_one_row:let conn = Connection::open(&db_path2).context("open DB to mark Sent")?;
apps/aberp/src/email_relay_daemon.rs|process_one_row:let conn = Connection::open(&db_path2).context("open DB to requeue")?;
apps/aberp/src/email_relay_daemon.rs|read_row_helper:let conn = Connection::open(db_path).context("open DB")?;
apps/aberp/src/email_relay_daemon.rs|reconcile_startup:let conn = Connection::open(&db_path).with_context(|| {
apps/aberp/src/email_relay_daemon.rs|write_relay_audit:let mut conn = Connection::open(&db_path).context("open DB for email-relay a
apps/aberp/src/export_invoice_bundle.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/incoming_invoices.rs|change_status:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/incoming_invoices.rs|change_status:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/incoming_invoices.rs|change_status:let mut conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/incoming_invoices.rs|get_incoming:let conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/incoming_invoices.rs|get_nav_xml_path:let conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/incoming_invoices.rs|ingest_incoming_invoice:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/incoming_invoices.rs|ingest_incoming_invoice:let mut conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/incoming_invoices.rs|list_incoming:let conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/incoming_invoices.rs|set_nav_xml_path:let conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/issue_invoice.rs|pre_tx_setup:let mut billing = DuckDbBillingStore::open(db_path)
apps/aberp/src/issue_modification.rs|modification_from_inputs:let ledger = Ledger::open(db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/issue_modification.rs|pre_tx_setup:let mut billing = DuckDbBillingStore::open(db_path)
apps/aberp/src/issue_storno.rs|pre_tx_setup:let mut billing = DuckDbBillingStore::open(db_path)
apps/aberp/src/issue_storno.rs|storno_from_inputs:let ledger = Ledger::open(db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/mark_abandoned.rs|resolve_stuck_or_loud_fail:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/mark_abandoned.rs|run:let ledger = Ledger::open(&args.db, tenant, binary_hash_bytes).context("open
apps/aberp/src/mark_abandoned.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/mark_abandoned.rs|run:let mut conn = Connection::open(&args.db)
apps/aberp/src/mark_invoice_paid.rs|mark_paid:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/mark_invoice_paid.rs|mark_paid:let ledger_for_check = Ledger::open(db_path, tenant.clone(), binary_hash)
apps/aberp/src/mark_invoice_paid.rs|mark_paid:let mut conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/material_inventory.rs|append_heat_lot_events:let mut ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/observe_receiver_confirmation.rs|load_base_nav_invoice_number:let store = DuckDbBillingStore::open(db_path)
apps/aberp/src/observe_receiver_confirmation.rs|run:let ledger = Ledger::open(&args.db, tenant, binary_hash_bytes)
apps/aberp/src/observe_receiver_confirmation.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/observe_receiver_confirmation.rs|run:let mut conn = Connection::open(&args.db)
apps/aberp/src/poll_ack.rs|lookup_transaction_id:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/poll_ack.rs|poll_ack_from_inputs:Connection::open(db).with_context(|| format!("open tenant DuckDB at {}", db.
apps/aberp/src/poll_ack.rs|poll_ack_from_inputs:let ledger = Ledger::open(db, tenant, binary_hash_bytes).context("open audit
apps/aberp/src/poll_ack.rs|write_daemon_terminal_ack:let ledger = Ledger::open(&inputs.db, inputs.tenant.clone(), inputs.binary_h
apps/aberp/src/poll_ack.rs|write_daemon_terminal_ack:let mut conn = Connection::open(&inputs.db).with_context(|| {
apps/aberp/src/poll_annulment_ack.rs|run:let ledger = Ledger::open(&args.db, tenant, binary_hash_bytes)
apps/aberp/src/poll_annulment_ack.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/poll_annulment_ack.rs|run:let mut conn = Connection::open(&args.db)
apps/aberp/src/print_invoice.rs|load_invoice_bank_snapshot:let mut conn = Connection::open(db)
apps/aberp/src/print_invoice.rs|load_invoice_notes:let mut conn = Connection::open(db)
apps/aberp/src/print_invoice.rs|render_to_bytes:let ledger = Ledger::open(db, tenant_id, binary_hash_bytes)
apps/aberp/src/quote_calibration.rs|append_calibration_events:let mut ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/quote_calibration.rs|record_calibration_for_completed_wo:let conn = Connection::open(db_path).context("open tenant DuckDB for calibra
apps/aberp/src/quote_pdf_rerender_daemon.rs|prepare_rerender:let conn = Connection::open(db_path)
apps/aberp/src/quote_pdf_rerender_daemon.rs|recover_unfinished_rerenders:let conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/quote_pdf_rerender_daemon.rs|write_audit:let mut conn = Connection::open(&db_path).context("open DB for pdf-rerender 
apps/aberp/src/quote_pricing_pipeline.rs|advance_extract:let mut conn = duckdb::Connection::open(&db_path).context("open DB for extra
apps/aberp/src/quote_pricing_pipeline.rs|advance_post:let conn = duckdb::Connection::open(&db).context("open DB for post")?;
apps/aberp/src/quote_pricing_pipeline.rs|advance_post:let mut conn = duckdb::Connection::open(&db_path).context("open DB for post-
apps/aberp/src/quote_pricing_pipeline.rs|advance_price:let mut conn = duckdb::Connection::open(&db_path).context("open DB for price
apps/aberp/src/quote_pricing_pipeline.rs|advance_render:let mut conn = duckdb::Connection::open(&db_path).context("open DB for rende
apps/aberp/src/quote_pricing_pipeline.rs|emit_daemon_panicked_audit:duckdb::Connection::open(&deps.db_path).context("open DB for daemon-panic au
apps/aberp/src/quote_pricing_pipeline.rs|emit_index_migrated_audit:let mut conn = duckdb::Connection::open(db_path).context("open DB for index-
apps/aberp/src/quote_pricing_pipeline.rs|emit_poll_outcome_audit:duckdb::Connection::open(&db_path).context("open DB for poll-outcome audit")
apps/aberp/src/quote_pricing_pipeline.rs|emit_python_resolved_audit:duckdb::Connection::open(db_path).context("open DB for python-resolved audit
apps/aberp/src/quote_pricing_pipeline.rs|enqueue_failed_no_cad:let conn = duckdb::Connection::open(&db_path).context("open DB for enqueue-f
apps/aberp/src/quote_pricing_pipeline.rs|enqueue_one:let mut conn = duckdb::Connection::open(&db_path).context("open DB for enque
apps/aberp/src/quote_pricing_pipeline.rs|next_actionable_blocking:let conn = duckdb::Connection::open(&db_path).context("open DB for next-job"
apps/aberp/src/recover_from_nav.rs|resolve_recovery_precondition:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/recover_from_nav.rs|run:let ledger = Ledger::open(&args.db, tenant, binary_hash_bytes)
apps/aberp/src/recover_from_nav.rs|run:let mut conn = Connection::open(&args.db)
apps/aberp/src/reports.rs|compute_financial_report:let conn = Connection::open(db_path)
apps/aberp/src/reports.rs|compute_financial_report:let conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/reports.rs|compute_financial_report:let ledger = Ledger::open(db_path, tenant.clone(), binary_hash)
apps/aberp/src/reports.rs|compute_financial_report:let mut store = aberp_billing::DuckDbBillingStore::open(db_path)
apps/aberp/src/request_technical_annulment.rs|load_base_invoice_issue_year:let store = DuckDbBillingStore::open(db_path)
apps/aberp/src/request_technical_annulment.rs|run:let ledger = Ledger::open(&args.db, tenant, binary_hash_bytes)
apps/aberp/src/request_technical_annulment.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/request_technical_annulment.rs|run:let mut conn = Connection::open(&args.db)
apps/aberp/src/restore_from_nav_extract.rs|open_for_extract:let conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/restore_from_nav_outgoing.rs|acquire_restore_lock_at:let conn = Connection::open(db_path)
apps/aberp/src/restore_from_nav_outgoing.rs|append_backfill_cycle_entry:let ledger = Ledger::open(&inputs.db_path, inputs.tenant.clone(), inputs.bin
apps/aberp/src/restore_from_nav_outgoing.rs|append_backfill_cycle_entry:let mut conn = Connection::open(&inputs.db_path)
apps/aberp/src/restore_from_nav_outgoing.rs|backfill_one_row:let conn = Connection::open(&db_path).with_context(|| {
apps/aberp/src/restore_from_nav_outgoing.rs|list_restored:let conn = Connection::open(db_path)
apps/aberp/src/restore_from_nav_outgoing.rs|list_restored_missing_buyer:let conn = Connection::open(db_path)
apps/aberp/src/restore_from_nav_outgoing.rs|load_already_restored_cache:let ledger = Ledger::open(db_path, tenant.clone(), binary_hash)
apps/aberp/src/restore_from_nav_outgoing.rs|process_digest:let ledger = Ledger::open(ctx.db_path, ctx.tenant.clone(), ctx.binary_hash)
apps/aberp/src/restore_from_nav_outgoing.rs|process_digest:let mut conn = Connection::open(ctx.db_path).with_context(|| {
apps/aberp/src/restore_from_nav_outgoing.rs|read_restore_lock_at:let conn = Connection::open(db_path)
apps/aberp/src/restore_from_nav_outgoing.rs|release_restore_lock_at:let conn = Connection::open(db_path)
apps/aberp/src/retry_submission.rs|perform_layer_2_check:let ledger_tx0 = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/retry_submission.rs|resolve_stuck_or_loud_fail:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/retry_submission.rs|run:let ledger = Ledger::open(&args.db, tenant, binary_hash_bytes)
apps/aberp/src/retry_submission.rs|run:let ledger = Ledger::open(&args.db, tenant, binary_hash_bytes)
apps/aberp/src/retry_submission.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/retry_submission.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/retry_submission.rs|run:let ledger_tx1 = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/retry_submission.rs|run:let mut conn = Connection::open(&args.db)
apps/aberp/src/serve.rs|archive_machine_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|archive_margin_profile_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|audit_event_detail_request:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|audit_events_request:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|build_poll_daemon_inputs:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|calibration_overview_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|cancel_dispatch_request:let mut conn = Connection::open(&*state.db_path).map_err(|e| {
apps/aberp/src/serve.rs|compute_workshop_dashboard:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|create_dispatch_request:let mut conn = Connection::open(&*state.db_path).map_err(|e| {
apps/aberp/src/serve.rs|create_machine_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|create_margin_profile_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|create_partner_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|create_product_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|create_stock_movement_request:if let Ok(ledger) = Ledger::open(&*state.db_path, state.tenant.clone(), bina
apps/aberp/src/serve.rs|create_stock_movement_request:let mut conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|create_work_order_request:let mut conn = Connection::open(&*state.db_path).map_err(|e| {
apps/aberp/src/serve.rs|decide_qa_inspection_request:let mut conn = Connection::open(&*state.db_path).map_err(|e| {
apps/aberp/src/serve.rs|delete_invoice_draft_request:let mut conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|delete_partner_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|delete_product_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|derive_state_for:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|get_audit_for_invoice:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|get_audit_for_quote:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|get_dispatch_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_invoice_detail:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|get_invoice_detail:let mut conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_invoice_draft_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_machine_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_partner_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_product_bom_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_product_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_product_with_inventory_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_qa_inspection_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|get_work_order_detail_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|handle_assign_heat_lot:let conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_create_complexity_rule:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_create_quoting_material:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_create_stock_adjustment:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_delete_complexity_rule:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_delete_quoting_material:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_delete_stock_adjustment:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_get_email_relay_row:let conn = duckdb::Connection::open(&db_path)
apps/aberp/src/serve.rs|handle_get_margin_profile:let conn = match Connection::open(&*state.db_path) {
apps/aberp/src/serve.rs|handle_get_parameters:let conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_get_quote_pricing_job_detail:let conn = duckdb::Connection::open(&db_path).context("open DB")?;
apps/aberp/src/serve.rs|handle_get_quote_pricing_job_pdf:let conn = duckdb::Connection::open(&db_path).context("open DB")?;
apps/aberp/src/serve.rs|handle_list_complexity_rules:let conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_list_email_relay_queue:let conn = duckdb::Connection::open(&db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_list_inventory_balances:let conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_list_quote_intake:let mut conn = duckdb::Connection::open(&db_path)
apps/aberp/src/serve.rs|handle_list_quote_pricing_jobs:let conn = duckdb::Connection::open(&db_path).context("open DB")?;
apps/aberp/src/serve.rs|handle_list_quoting_materials:let conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_list_stock_adjustments:let conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_list_tolerance_multipliers:let conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_quote_intake_mark_irrelevant:let conn = duckdb::Connection::open(&db_path)
apps/aberp/src/serve.rs|handle_quote_intake_notifications:let conn = duckdb::Connection::open(&db_path)
apps/aberp/src/serve.rs|handle_quote_intake_notifications:let ledger = Ledger::open(&db_for_ledger, tenant_for_ledger, binary_hash)
apps/aberp/src/serve.rs|handle_quote_intake_retry_parse:let conn = duckdb::Connection::open(&db_path)
apps/aberp/src/serve.rs|handle_quote_pipeline_status:let conn = duckdb::Connection::open(&db_path).context("open DB for daemon-pa
apps/aberp/src/serve.rs|handle_relay_send_email:let conn = duckdb::Connection::open(&db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_retry_quote_pricing_job:let mut conn = duckdb::Connection::open(&db_path).context("open DB")?;
apps/aberp/src/serve.rs|handle_test_catalogue_push:let conn = Connection::open(&db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_update_complexity_rule:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_update_parameters:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_update_quoting_material:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_update_stock_adjustment:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|handle_update_tolerance_multiplier:let mut conn = Connection::open(&*state_for_task.db_path).with_context(|| {
apps/aberp/src/serve.rs|latest_quote_intake_poll:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|list_dispatches_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_eligible_work_orders_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_invoice_drafts_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_invoices:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|list_invoices:let mut conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_low_stock_products_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_machines_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_margin_profiles_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_notes_history_request:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|list_partners_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_products_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_products_with_inventory_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_qa_inspections_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_stock_movements_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|list_work_orders_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|mark_dispatch_shipped_request:let mut conn = Connection::open(&*state.db_path).map_err(|e| {
apps/aberp/src/serve.rs|override_lead_time_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|override_quote_margin_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|pickup_quote_as_draft_request:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|pickup_quote_as_draft_request:let mut conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|put_product_bom_request:let mut conn = Connection::open(&*state.db_path).map_err(|e| {
apps/aberp/src/serve.rs|query_non_terminal_invoices:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|quote_intake_auth_paused:let ledger = Ledger::open(&*state.db_path, state.tenant.clone(), binary_hash
apps/aberp/src/serve.rs|read_base_currency:let mut conn = Connection::open(db_path)
apps/aberp/src/serve.rs|read_invoice_currency:let mut conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/serve.rs|read_invoice_total_gross_minor:let mut conn = Connection::open(db_path).with_context(|| {
apps/aberp/src/serve.rs|record_upgrade_snapshot_mismatch_audit:let mut ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/serve.rs|resolve_customer_identity:let ledger = Ledger::open(
apps/aberp/src/serve.rs|resolve_invoice_xml_path:let ledger = Ledger::open(
apps/aberp/src/serve.rs|resolve_recipient_email:let mut conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|run:duckdb::Connection::open(&cad_db_path).context(
apps/aberp/src/serve.rs|run:duckdb::Connection::open(&count_db_path)
apps/aberp/src/serve.rs|run:duckdb::Connection::open(&migrate_db_path)
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let mut billing_store = DuckDbBillingStore::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let mut conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let mut conn = Connection::open(&args.db).with_context(|| {
apps/aberp/src/serve.rs|run:let mut store = DuckDbBillingStore::open(creating)?;
apps/aberp/src/serve.rs|run_deal_saga_request:let mut conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|run_refuse_saga_request:let mut conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|set_quote_buyer_partner_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|sync_audit_mirror_best_effort:if let Ok(ledger) = Ledger::open(db_path, tenant, binary_hash) {
apps/aberp/src/serve.rs|transition_routing_op_request:let mut conn = Connection::open(&*state.db_path).map_err(|e| {
apps/aberp/src/serve.rs|transition_work_order_request:let mut conn = Connection::open(&*state.db_path).map_err(|e| {
apps/aberp/src/serve.rs|update_machine_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|update_margin_profile_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|update_partner_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/serve.rs|update_product_request:let conn = Connection::open(&*state.db_path)
apps/aberp/src/snapshot.rs|open_ledger:Ledger::open(db_path, tenant.clone(), binary_hash)
apps/aberp/src/submission_queue.rs|count_pending:let ledger = Ledger::open(db_path, tenant, binary_hash)
apps/aberp/src/submit_annulment.rs|run:let ledger = Ledger::open(&args.db, tenant, binary_hash_bytes)
apps/aberp/src/submit_annulment.rs|run:let ledger = Ledger::open(&args.db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/submit_annulment.rs|run:let mut conn = Connection::open(&args.db)
apps/aberp/src/submit_invoice.rs|submit_from_inputs:let ledger = Ledger::open(db, tenant.clone(), binary_hash_bytes)
apps/aberp/src/submit_invoice.rs|submit_from_inputs:let mut conn = Connection::open(db)
crates/aberp-inventory/src/bin/rebuild_stock_cache.rs|run:let mut conn = Connection::open(&db_path)
crates/aberp-mes/src/ledger_writer.rs|write_one:let mut conn = duckdb::Connection::open(&db_path)
crates/aberp-quote-intake/src/service.rs|process_one_quote:let conn = duckdb::Connection::open(&db_for_check).map_err(|e| {
crates/aberp-quote-intake/src/service.rs|process_one_quote:let conn = duckdb::Connection::open(&db_for_err).map_err(|e| {
crates/aberp-quote-intake/src/service.rs|process_one_quote:let conn = duckdb::Connection::open(&db_for_insert).map_err(|e| {
crates/aberp-quote-intake/src/service.rs|process_one_quote:let conn = duckdb::Connection::open(&db_for_mark).map_err(|e| {
crates/aberp-quote-intake/src/service.rs|retry_pending_writebacks:let conn = duckdb::Connection::open(&db_for_mark).map_err(|e| {
crates/aberp-quote-intake/src/service.rs|snapshot_pending_writebacks:let conn = duckdb::Connection::open(&db_for_list)
crates/aberp-quote-intake/src/service.rs|write_cycle_audit:let mut conn = duckdb::Connection::open(&db_path)
crates/aberp-quote-intake/src/service.rs|write_row_added:let mut conn = duckdb::Connection::open(&db_path).map_err(|e| {
crates/aberp-snapshot/src/crash_safe.rs|checkpoint_file:let conn = Connection::open(db)?;
crates/aberp-snapshot/src/crash_safe.rs|probe_open:let conn = Connection::open(db_path).map_err(|e| e.to_string())?;
crates/aberp-snapshot/src/take.rs|restore_into:let conn = Connection::open(&staging)?;
crates/aberp-snapshot/src/take.rs|take_snapshot:let conn = Connection::open(db_path)?;
crates/audit-ledger/src/storage/mod.rs|append_reopen:let mut conn = Connection::open(db_path)?;
crates/audit-ledger/src/storage/mod.rs|append_reopen:pub fn append_reopen(
crates/audit-ledger/src/storage/mod.rs|open:let conn = Connection::open(path)?;
modules/billing/src/adapters/duckdb_store.rs|open:conn: Connection::open(path)?,
