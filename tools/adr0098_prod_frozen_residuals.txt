# ADR-0099 (PROD SAFE lane) — FROZEN runtime independent-opener census.
# Ported from the editions ADR-0098 C2 mechanism (CHECK 10i) to the PROD tree.
# Format: <count> <path>.
#
# The gate (tools/cut_gate_opener_census.sh, CHECK P1) re-derives each file's
# RUNTIME opener count via the shared comment/string/cfg(test)-aware scanner
# (tools/adr0098_opener_scan.awk: Connection::open(_with_flags)? / Ledger::open /
# DuckDbBillingStore::open / Database::open / append_reopen; open_in_memory &
# from_connection excluded) and FAILS if any file EXCEEDS its frozen count, or if
# a NEW opener-bearing file appears that is not listed here.
#
# THIS IS THE PRE-H3 CENSUS. Every opener below is CURRENTLY ALLOWED. The gate
# does NOT (yet) require zero openers or a shared Handle — that is the durability
# work (H1 "mirror preserve-and-refuse" + H3 "shared Handle"), a FOLLOW-UP prod
# session. This gate's sole job right now is to FREEZE the census so the opener
# surface cannot silently GROW while durability lands. NO seam exclusions:
# pre-durability there is no aberp-db Handle, so every runtime opener is a frozen
# residual (contrast editions, which excluded the Handle/snapshot seams).
#
# Scope: apps/aberp/src + modules + crates, minus */tests/*. Machine-derived from
# frozen prod commit f7519b4 (tag PROD_v2.27.76). Re-derive: see cut_gate_opener_census.sh.
#
# ADR-0099 H2 (Class 3 atomic-creation + safe-open) legitimately alters the boot
# open path, so the frozen census is ratcheted +3 (289→292 across 42→43 files):
#   • apps/aberp/src/serve.rs 144→145 — the atomic-provision seed open
#     (`DuckDbBillingStore::open(creating)`) that replaces the old implicit
#     torn-creation of the DB at the live path.
#   • crates/aberp-snapshot/src/crash_safe.rs +2 (NEW file) — the backported
#     `checkpoint_file` fold-open + the `probe_open` validated boot safe-open.
# These are the atomic/probe path G2 permits; NOT new un-gated openers.
#
# ADR-0099 Addendum 2 (2026-07-09, owner Ervin) — owner-approved surgical NAV
# recovery fix (Defect 3) migrates the drain-pending-retries per-invoice openers
# onto the live handle (free sync_mirror + Ledger::from_connection, both census-
# excluded), ahead of the wider H3 migration, because the re-open was an active
# duckdb#23046 corruption hazard on the operator's machine. The census RATCHETS
# DOWN −7 (292→285 across 43 files, no file dropped to zero): 5 removed from
# drain_pending_retries.rs (8→3), 1 from recover_from_nav.rs (4→3), 1 from
# retry_submission.rs (9→8) — the three deleted `derive_nav_invoice_number`
# billing-store opens (Defect 1) plus the four reused drain Ledger::open sites
# (Defect 3). Per the H3 "Remainder" note, each opener removal is re-cut into
# this census + the fingerprint set in the SAME change.
#
# ADR-0099 H3 ratchet: serve.rs audit write-forks migrated onto the shared
# aberp_db::Handle. Census ratcheted DOWN 285->271 / files 43. Re-derive: see
# cut_gate_opener_census.sh. (P1 already forbids GROWTH; this is a sanctioned shrink.)
#
# ADR-0099 H3 ratchet (wave 2a, email_outbox + catalogue_push fully migrated): 267 openers / 41 files.
#
# ADR-0099 H3 ratchet (wave 2b, mes_manager): 266 openers / 40 files.
# ADR-0099 H3 wave-2c (2026-07-09, owner Ervin): the avl_vendors subsystem is
# fully migrated onto the shared Handle — all 7 serve.rs AVL handlers (create /
# list / get / update / set-status / screen / po-check), append_vendor_event,
# and fire_overdue_screening_reminders now route READS and WRITES through the ONE
# Handle. Re-cut −9 (266→257 openers, 40→39 files; avl_vendors.rs drops off,
# serve.rs 131→124). Removals-only (0 additions), verified via comm.
# ADR-0099 H3 wave-2d (2026-07-09): email_invoice::record_email_audit_entry
# migrated onto the shared Handle via the verify_chain recipe (append + try_clone
# from_connection verify + guard-drop mirror). email_invoice.rs drops off. Re-cut
# -2 (257->255 openers, 39->38 files). Removals-only, verified via comm.
# ADR-0099 H3 wave-2e (2026-07-09): append_machine_event (the CROSS-SUBSYSTEM
# shared audit helper — 12 callers across machines/partners/margins/lead-time/
# quote-margin/reprice) migrated AUDIT-ONLY onto the shared Handle. Business opens
# stay put (coherent [business-fold, audit-append] per handler). Re-cut -1
# (255->254 openers, 38->37 files; quoting_machines.rs drops off). Removals-only.
# ADR-0099 H3 wave-3a (ap_sync family, atomic): the IncomingInvoiceSyncCycleCompleted
# writer (write_cycle_audit_entry_inner, verify_chain recipe) + its sentinel reader
# (bootstrap_year_already_recorded) both onto the shared Handle in ONE commit
# (family invariant, [[aberp-h3-handle-coherence-model]]). ap_sync.rs drops off.
# Removals-only.
# ADR-0099 H3 wave-3b (quote_pdf_rerender_daemon family, atomic): write_audit (writer)
# + recover_unfinished_rerenders (raw-SQL FROM audit_ledger reader) onto the Handle.
# prepare_rerender business open stays. Removals-only.
# H3 wave-3c (2026-07-10) — re-cut −12 (removals-only, comm-verified 0 additions):
# restore_from_nav migrated onto the shared Handle (restore_from_nav_outgoing.rs −11,
# restore_from_nav_extract.rs open_for_extract −1). 249/36 → 237/34.
# H3 invoice-family (2026-07-10) — re-cut -29 (removals-only, comm-verified 0
# additions): the invoice-family atomic migration onto the shared Handle. 271-era
# census 237/34 -> 208/27. 16 writer openers (issue_invoice/issue_storno/
# issue_modification/submit_invoice/poll_ack/mark_invoice_paid + submission_queue::
# count_pending) + 13 serve.rs invoice-audit/billing READER openers (list_invoices
# +billing conn, get_invoice_detail +billing conn, get_audit_for_invoice,
# list_notes_history_request, derive_state_for, query_non_terminal_invoices,
# build_poll_daemon_inputs, audit_events_request, audit_event_detail_request,
# resolve_customer_identity, resolve_invoice_xml_path) removed -> all on the Handle.
# H3 master-data partners subsystem (2026-07-10) — re-cut -5 (removals-only,
# comm-verified 0 additions): the /api/partners CRUD surface migrated onto the
# shared Handle. Writers create/update/delete_partner_request -> state.db.write();
# readers list_partners_request/get_partner_request -> state.db.read() (a
# fresh-open reader opens a SEPARATE DuckDB instance that checkpoints on close and
# truncates the Handle's audit WAL tail — proven a shutdown-tear truncator by the
# live NAV-off reproducer). serve.rs 111->106. (Products stay fresh-open: their
# with-inventory reader reads products.stock_qty written by 8 WO/dispatch/QA
# record_movement callers, so the product closure pulls in the inventory+WO
# surface — handed off as its own atomic commit.)
# ── H3 STEP 7 (2026-07-13) — ACCEPTANCE-GATE reconciliation re-cut ─────────────
# The write-fork + read-fork migration reached ZERO on integrate/dur-feat; the
# gates flip to ENFORCE this step. The frozen baseline above was pinned pre-H3
# (f7519b4) AND pre-feature-merge, so it legitimately diverges from the current
# tree. Full REVIEWED re-cut: 271-era census (see prior notes) → 117 openers / 23
# files. Reconciled delta vs the immediately-prior frozen set (184/25):
#   REMOVALS (73, all Handle migrations, comm-verified): incoming_invoices.rs 9→0,
#     material_inventory.rs 1→0, quote_calibration.rs 2→0,
#     aberp-quote-intake/service.rs 8→0, serve.rs 106→55 (−51). Each removed opener
#     now routes through the shared aberp_db::Handle (db.write()/db.read()); the
#     write-fork CHECK M and read-fork gates independently confirm no half-migration.
#   ADDITIONS (6, NONE a smuggled fresh-open fork; write-fork & read-fork = 0):
#     • audit_dap_boot.rs:run_heartbeat_supervised (+1) and
#       serve.rs:spawn_dap_audit_chain (+1) — the DÁP/QES boot + heartbeat. Both
#       are cross-fn (open here, append in open_service_session_and_recover /
#       heartbeat), so the per-fn write-fork scanner is BLIND to them. TRACKED
#       RESIDUAL, SECURED: a PRODUCTION build refuses to start when dap_enabled=true
#       (serve.rs DÁP boot arm, build_profile::IS_PRODUCTION_BUILD) → never ARMED
#       in prod. Precisely: a runtime `if` on a const, not a `cfg`, so the branch is
#       not-taken rather than the code unreachable. See
#       tools/adr0099_write_fork_allowlist.txt residual note.
#     • serve.rs:seed_demo_sample_data (+1) — S434 boot-time, best-effort seed of the
#       SEPARATE bundled-demo DB (not the booted tenant's Handle, not in-serve). A
#       legitimate boot opener, not a fork.
#     • tenant_registry.rs ×3 (+3) — WITHDRAWN 2026-07-21, see the lexer-fix note
#       at the foot of this header. These were scanner false positives, and the
#       attribution above them was itself wrong: the raw string at :922 is INSIDE
#       the swallowed window, not its cause.
# ── H4 backlog-2 (2026-07-14) — pricing-config family onto the shared Handle ───
# margin_profiles / quoting_materials / quoting_tunables: the 22 CRUD+list serve
# handlers (create/update/archive/list/get margin profiles; create/update/delete/
# list materials + the test-push list_public read; create/update/delete/list
# complexity rules, list/update tolerance multipliers, get/update parameters,
# create/update/delete/list stock adjustments) migrated off fresh Connection::open
# onto state.db.write()/.read(). Writers fuse the business INSERT/UPDATE + audit
# append in ONE Handle tx (canonical recipe, rule 15; margins previously did a
# business-commit-then-append_machine_event split). Closes a real Q2-stale
# fail-open: the pricing pipeline reads this config THROUGH the Handle, so a fresh
# writer's edit was invisible to it (margin floor priced on stale/empty config).
# Re-cut −22 (removals-only, comm-verified 0 additions): serve.rs 55→33; census
# 117→95 openers, files unchanged (23). write-fork & read-fork gates stay = 0.
# Same session folded in quoting_machines (the IDENTICAL half-migration — pipeline
# reads list_enabled_capacities via the Handle while serve wrote machine CRUD fresh
# → lead-time on stale capacity; do-not-defer a known fork per 4e). Writers
# create/update/archive_machine_request fuse business+audit on ONE Handle guard
# (was Connection::open + a separate append_machine_event guard); readers list/
# get_machine_request → db.read(). calibration_overview_request STAYS fresh (reads
# quote_calibration, a fresh-open residual-floor family — migrating its read alone
# would open a NEW half-migration). Re-cut −5 more: serve.rs 33→28; census 95→90.
# ── VAT stack (ADR-0101 + ADR-0102, 2026-07-16) — REVIEWED census GROWTH +1 ────
# The vat-rate-kind feature adds exactly ONE new prod DB opener across the scoped
# tree: serve.rs:read_base_line_vat_kinds (ADR-0101 S2 modification-guard, reads the
# persisted invoice_line.vat_rate_kind to reject in-app modification of a non-Percent
# base). Pattern-identical twin of the frozen billing reader read_invoice_total_gross_minor
# (same fresh Connection::open + scoped read-tx, same billing::load_ready_invoice_by_id,
# BILLING data — its own DuckDbBillingStore, NOT the shared audit Handle, READ-ONLY).
# A like-for-like add to the census-approved fresh-open billing-reader residual family,
# NOT a fork: write-fork & read-fork ENFORCE gates stay = 0. Re-cut +1: serve.rs 28→29;
# census 90→91 openers, files unchanged (23). See the fingerprint-set header for the
# per-opener rationale. (The ADR-0102 EU-partner base read reuses existing openers.)
#
# ── 2026-07-19 SNAPSHOT-DAEMON HANDLE MIGRATION — census RATCHETS DOWN 91→89 ──
# Root-cause fix for the prod boot refusal (audit mirror seq 8060 > DB seq 8058,
# hash fork; third occurrence in two weeks). The snapshot daemon was the last
# in-serve audit writer off the shared Handle. TWO openers REMOVED, zero added:
#   - apps/aberp/src/snapshot.rs|open_ledger:Ledger::open(...)        [1 → 0, file drops out]
#     Per-event audit ledger open. Its `append` took AUDIT_APPEND_LOCK while
#     Handle writers use append_in_tx (a DISJOINT mutex) and there is no
#     UNIQUE(seq) since S341 — so both could read head 8055 and both write 8056.
#     Now db.write() -> append_in_tx -> commit, on the one writer mutex.
#   - crates/aberp-snapshot/src/take.rs|take_snapshot:Connection::open(db_path)  [2 → 1]
#     The EXPORT DATABASE opener. It lacked the Handle`s checkpoint_enabled:false
#     + disable_checkpoint_on_shutdown, so closing it FOLDED the Handle`s WAL
#     after sync_mirror had durably appended — the mirror-ahead half of the
#     incident. It also risked exporting a stale pre-WAL view. take_snapshot now
#     takes a CALLER-supplied connection (Handle::read(), a try_clone of the one
#     instance): coherent by construction, and closing it folds nothing.
# Census 91→89 openers, files 23→22 (both snapshot.rs and one take.rs opener
# gone). Ratchet DOWN only — no additions, no content swaps. write-fork &
# read-fork ENFORCE gates stay = 0.
# ── LEXER FIX (2026-07-21) — the scanner was FAIL-OPEN; census 89→86 / 22→21 ──
# tools/adr0098_opener_scan.awk had no char-literal rule. `out.push('"')` at
# tenant_registry.rs:615/:623 flipped the string state ON, leaving the lexer
# stuck mid-string from :623 to :1092 — a 470-line window in which it saw
# NOTHING. That window contains `mod tests {` (:880), so the 3 test-only
# Ledger::open calls at :1280/:1298/:1311 were frozen here as runtime residuals.
# The dangerous half is the other one: a genuine runtime opener added anywhere
# in that window would have been invisible to a gate whose whole job is to see
# it. 17 in-scope files carry quote char literals, serve.rs among them.
# Blast radius, measured old-vs-new over all 272 in-scope files: the ONLY delta
# is tenant_registry.rs 3→0. Cross-checked independently of the lexer — all 155
# raw opener-token occurrences in scope classify as 86 runtime (unchanged), 64
# inside #[cfg(test)], 5 in doc/line comments. NO real opener was hiding.
# Delta, line by line — three removals, zero additions:
#   −3  apps/aberp/src/tenant_registry.rs (file drops off the census entirely)
#       −1 …|tenant_created_lands_in_new_tenant_ledger_not_caller:Ledger::open(&db_caller, …).unwrap();
#       −1 …|tenant_created_lands_in_new_tenant_ledger_not_caller:let new_kinds: Vec<EventKind> = Ledger::open(&db_new, …
#       −1 …|tenant_created_lands_in_new_tenant_ledger_not_caller:Ledger::open(&db_caller, …)
#       all three: FALSE POSITIVE REMOVED (test-only, never runtime).
# The lexer now also handles raw strings (r#"…"# incl. multi-line + hash width),
# nested block comments, and keeps char-literal bodies out of the brace counter
# so '{' / '}' (numbering.rs:649/:653) cannot skew cfg(test) depth. Lifetimes and
# loop labels (&'a str, 'static, 'outer:) are explicitly NOT char literals — the
# classic way a naive fix breaks. Pinned by the two [lexer traps] negative probes
# in tools/cut_gate_negative_probes.sh; reverting the awk fix turns them RED.
1 apps/aberp/src/audit_dap_boot.rs
3 apps/aberp/src/drain_pending_retries.rs
6 apps/aberp/src/drain_submission_queue.rs
1 apps/aberp/src/export_invoice_bundle.rs
4 apps/aberp/src/mark_abandoned.rs
4 apps/aberp/src/observe_receiver_confirmation.rs
3 apps/aberp/src/poll_annulment_ack.rs
3 apps/aberp/src/print_invoice.rs
1 apps/aberp/src/quote_pdf_rerender_daemon.rs
3 apps/aberp/src/recover_from_nav.rs
4 apps/aberp/src/reports.rs
4 apps/aberp/src/request_technical_annulment.rs
8 apps/aberp/src/retry_submission.rs
29 apps/aberp/src/serve.rs
3 apps/aberp/src/submit_annulment.rs
1 crates/aberp-inventory/src/bin/rebuild_stock_cache.rs
1 crates/aberp-mes/src/ledger_writer.rs
2 crates/aberp-snapshot/src/crash_safe.rs
1 crates/aberp-snapshot/src/take.rs
3 crates/audit-ledger/src/storage/mod.rs
1 modules/billing/src/adapters/duckdb_store.rs
